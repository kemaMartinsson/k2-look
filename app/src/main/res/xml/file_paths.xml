<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External storage downloads directory for APK updates -->
    <external-path
        name="downloads"
        path="Download/" />
    <!-- External files directory (no permission needed) -->
    <external-files-path
        name="external_files"
        path="." />
    <!-- Cache directory for temporary files -->
    <cache-path
        name="cache"
        path="/" />
</paths>

