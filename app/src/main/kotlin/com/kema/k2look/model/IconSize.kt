package com.kema.k2look.model

/**
 * Icon size for display on glasses
 */
enum class IconSize(val pixels: Int) {
    SMALL(28),  // 28×28 icons for compact layouts
    LARGE(40)   // 40×40 icons for prominent display
}

